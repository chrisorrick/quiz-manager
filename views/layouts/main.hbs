<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />

  <link rel="stylesheet" href="/styles/fa.min.css">
  <link rel="stylesheet" href="/styles/main.css">
  {{!-- <script src="https://kit.fontawesome.com/e2df984236.js" crossorigin="anonymous"></script> --}}
  <script src="/js/scripts.js"></script>
  
  {{#if title}}
    <title>{{title}} | Quiz Manager</title>
  {{else}}
    <title>Quiz Manager</title>
  {{/if}}
  
</head>
<body>
  {{#unless hide-header}}
    {{> header}}
  {{/unless}}

  {{#if app-page}}
    <div class="app-wrapper">
  {{/if}}

  <div class="container {{#if section-slug}}{{section-slug}}-container{{/if}}">
	  {{{body}}}
  </div>

  {{#if app-page}}
    </div>
  {{/if}}

  {{#unless hide-footer}}
    {{> footer}}
  {{/unless}}
</body>
</html>